package com.krishanagarwal.mynoo.ui.theme

import androidx.compose.ui.graphics.Color

// ── Brand palette (mirrors T constants from the React Native app) ─────────────

/** Warm cream — all child-screen backgrounds */
val MynooBackground = Color(0xFFFFF9F0)
val MynooCard       = Color(0xFFFFFFFF)

/** Warm orange-red — headers, CTA, active tab */
val MynooOrange     = Color(0xFFFF6B35)

/** Mint teal — secondary actions, calendar dots, play buttons */
val MynooTeal       = Color(0xFF4ECDC4)

/** Sunshine yellow — dividers, highlights */
val MynooYellow     = Color(0xFFFFE66D)

/** Soft mint — avatar rings, active word highlight */
val MynooMint       = Color(0xFFB5EAD7)

/** Success / session-time green */
val MynooGreen      = Color(0xFF6BCF7F)

/** Error / end-session red */
val MynooRed        = Color(0xFFFF6B6B)

// ── Text ──────────────────────────────────────────────────────────────────────
val TextDark  = Color(0xFF2C3E50)
val TextMid   = Color(0xFF4A6572)
val TextLight = Color(0xFF7F8C8D)

// ── Chrome ────────────────────────────────────────────────────────────────────
val MynooBorder  = Color(0xFFF0E6D3)
val MynooDivider = Color(0xFFF0F0F0)

// ── Language-semantic accent colours ─────────────────────────────────────────
val LangEnglish = Color(0xFF27AE60)
val LangHindi   = Color(0xFFE67E22)
val LangPunjabi = Color(0xFF8E44AD)

// ── Material3 seed colours ────────────────────────────────────────────────────
val Seed = MynooOrange
